{
  "name": "jsontrail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Compare JSON structures instead of lines. Explore changed paths and export a JSON Patch.",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "start": "node scripts/serve.mjs",
    "format": "prettier --write src scripts tests"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "prettier": "3.8.1"
  }
}
